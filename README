First run
1. Build docker image from within Docker directory: docker build -t sidkris/sim_ldhat:latest .
2. Within the bin directory run chmod +x *.py

Launch
nextflow run sim_LDhat.nf -with-report report.html

NOTE
The program complete has been renamed to ldhat_complete. To avoid conflict with the system program of the same name